#include "libft.h"

void	ft_putchar(char c);

void	ft_putnbr(int nb)
{
  unsigned int nbu;

  if (nb < 0)
    {
      ft_putchar('-');
      nb = -nb;
    }
  nbu = nb;
  if (nbu >= 10)
    {
      ft_putnbr(nbu / 10);
      ft_putnbr(nbu % 10);
    }
  if (nbu < 10)
    ft_putchar(nbu + '0');
}
